# Keep every pip invocation in the same CUDA/Python dependency world.
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

# Stable CUDA 12.8 stack. PyTorch publishes this trio as a matched release set.
torch==2.11.0+cu128
torchvision==0.26.0+cu128
torchaudio==2.11.0+cu128

# Inline Studio v1.2.63 requires diffusers 0.39 for Krea 2.
diffusers==0.39.0
transformers==4.57.6
tokenizers==0.22.2
accelerate==1.14.0
huggingface-hub==0.36.0
safetensors==0.7.0
sentencepiece==0.2.1

# Numerics/media pins shared with the current image family.
numpy==2.2.6
scipy==1.17.0
Pillow==12.1.0
opencv-python-headless==4.12.0.88
einops==0.8.1

# Packaging floor/ceiling used by the current base image.
setuptools>=66.1,<82
wheel>=0.38
